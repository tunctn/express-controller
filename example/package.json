{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc && npx swc src --out-dir dist",
    "dev": "npx swc src --out-dir dist --watch"
  },
  "dependencies": {
    "@express-controller/auth-middleware": "workspace:*",
    "@express-controller/core": "workspace:*",
    "@express-controller/redis-middleware": "workspace:*",
    "@express-controller/validator-middleware": "workspace:*",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/express-serve-static-core": "^4.19.5",
    "@types/qs": "^6.9.15"
  }
}
